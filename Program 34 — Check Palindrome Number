using System;
class Program {
    static void Main() {
        int num = 121, temp = num, rev = 0;
        while (temp > 0) {
            rev = rev * 10 + temp % 10;
            temp /= 10;
        }
        Console.WriteLine(num == rev ? "Palindrome" : "Not Palindrome");
    }
}
